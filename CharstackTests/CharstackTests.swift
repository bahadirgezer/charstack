import Testing
@testable import Charstack

@Suite("Charstack Tests")
struct CharstackTests {

    @Test("App launches without errors")
    func appSmokeTest() {
        // Placeholder — verifies the test target compiles and links against the app.
        #expect(true)
    }
}
